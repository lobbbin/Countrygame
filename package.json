{
  "name": "online-store",
  "version": "1.0.0",
  "private": true,
  "description": "Fullstack online store: Node.js/Express + SQLite backend with JWT auth, React/Vite frontend, product catalog, cart & checkout.",
  "scripts": {
    "dev": "concurrently -k -n backend,frontend \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "node backend/server.js",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "seed": "node backend/src/seed.js",
    "start": "node backend/src/seed.js && node backend/server.js"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}